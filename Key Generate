import random
def randomprime():
    global primenum
    primenum= random.randrange(20, 200)
    
    for i in range(2, primenum):
        if primenum % i == 0:
            randomprime()
            break
    else:
        return
    
def diffrentnum():
    global q
    randomprime()
    q = primenum
    if q == p:
        diffrentnum()
    else:
        return

import random

def keygenerate():
    randomprime()
    p = primenum
    diffrentnum()
    print (p , q)
